$(document).ready(function() {
	/* Activación del slider */
	$(document).ready(function() {
	  $('.slider').slick({
	    autoplay: true,
	    autoplaySpeed: 2000,
	    speed: 500,
	    dots: true,
	    arrows: true,
	  });
	});
});
